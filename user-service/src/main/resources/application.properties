spring.application.name=user-service
server.port=8081

# Local MySQL (already configured on this machine).
spring.datasource.url=jdbc:mysql://mysql-1844b3d8-vidyasghatani-e6d3.a.aivencloud.com:28892/registration_db?sslMode=REQUIRED&serverTimezone=UTC
spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Tables already exist in the local MySQL database (registration_db.users) - never let Hibernate create/drop them.
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect